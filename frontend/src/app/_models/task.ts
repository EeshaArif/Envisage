export interface Task {
  _id: string;
  description: string;
  start_date: string;
  end_date: string;
  priority: string;
  status: string;
}
export interface TaskData {
  description: string;
  start_date: string;
  end_date: string;
  priority: string;
  status: string;
}
